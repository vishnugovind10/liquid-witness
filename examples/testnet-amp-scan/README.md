# Testnet AMP Scan Example

This directory contains the committed v0.1.0 demonstration artifact.

The artifact is deliberately marked `DEMO` because no public AMP testnet asset with a reproducible descriptor was validated for this release.
